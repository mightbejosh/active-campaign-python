#!/usr/bin/env python3

from distutils.core import setup

setup(name='Active Campaign',
      version='2.0',
      packages=['activecampaign'],
      install_requires=[
          'simplejson',
      ],
      )
